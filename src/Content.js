const Content = () => {
    const randomName = () => {
        const names = ["Azeez", "Ajibola","Aremu"];
        const int = Math.floor(Math.random() * 3);
        return names[int];
      }
    return (
        <main>
            
        </main>
    )
}

export default Content
